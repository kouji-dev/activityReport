import { FC } from "react";
import { Id } from "utils/types";

interface Props {
  activityReportId: Id;
}

export const ValidateToolboxCell: FC<Props> = () => {
  return <div>Cell</div>;
};
