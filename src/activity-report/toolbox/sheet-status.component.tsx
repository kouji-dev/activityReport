import { SheetStatus } from "activity-report/sheet-status";
import { FC } from "react";

interface Props {
  status: SheetStatus;
}

export const SheetStatusItem: FC<Props> = () => {
  return <div>hi</div>;
};
